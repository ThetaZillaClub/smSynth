import type { CourseDef } from "../types";

const PITCH_TUNE_COURSE: CourseDef = {
  slug: "pitch-tune",
  title: "Pitch Tune",
  subtitle: "Single-pitch call & response",
  lessons: [], // placeholders fine
};

export default PITCH_TUNE_COURSE;
