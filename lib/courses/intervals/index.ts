import type { CourseDef } from "../types";

const INTERVALS_COURSE: CourseDef = {
  slug: "intervals",
  title: "Intervals",
  subtitle: "Hear & sing steps",
  lessons: [],
};

export default INTERVALS_COURSE;
