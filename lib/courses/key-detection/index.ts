import type { CourseDef } from "../types";

const KEY_DETECTION_COURSE: CourseDef = {
  slug: "key-detection",
  title: "Key Detection",
  subtitle: "Find the key by ear",
  lessons: [],
};

export default KEY_DETECTION_COURSE;
